# CWB Hub Hybrid AI System Requirements
# Versões atualizadas para evitar problemas de compilação

# Core dependencies
asyncio-mqtt>=0.16.1,<1.0.0
dataclasses-json>=0.6.3,<1.0.0
pydantic>=2.11.0,<3.0.0  # Versão mais recente com wheels pré-compilados
typing-extensions>=4.8.0,<5.0.0

# Logging and monitoring
structlog>=23.2.0,<26.0.0
prometheus-client>=0.19.0,<1.0.0

# Development dependencies
pytest>=7.4.3,<9.0.0
pytest-asyncio>=0.21.1,<1.0.0
pytest-cov>=4.1.0,<6.0.0
black>=23.11.0,<25.0.0
flake8>=6.1.0,<8.0.0
mypy>=1.7.1,<2.0.0

# Optional dependencies for enhanced features
# Uncomment if needed:
# openai>=1.3.0,<2.0.0  # For AI model integration
# langchain>=0.0.350,<1.0.0  # For advanced NLP
# redis>=5.0.1,<6.0.0  # For caching
# psycopg2-binary>=2.9.9,<3.0.0  # For PostgreSQL

# Platform-specific notes:
# - pydantic>=2.11.0 includes pre-compiled wheels for most platforms
# - If compilation issues persist, install Rust: https://rustup.rs/
# - Alternative: use conda instead of pip for complex dependencies